# Scripts

This folder is reserved for repository-level helper scripts.

Current root scripts are defined in `package.json`:

```bash
npm run engine:start
npm run engine:server
npm run engine:cli -- help
npm run bi -- help
npm run check
npm test
```

Future helper scripts may include:

- endpoint documentation generator
- release checklist runner
- trace sanitizer
- fixture generator
- local smoke test runner
